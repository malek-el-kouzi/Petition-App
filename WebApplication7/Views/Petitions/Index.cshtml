@model IEnumerable<WebApplication7.Models.Petition>

@{
    
}


<div class="container">
    <div class="text-center text-light">
        <h2 class="text-center mb-3 mt-3 text-dark">@ViewData["Title"]</h2>
         <hr />
       
    </div>
    <div class="row row-cols-1 row-cols-md-2">
        @foreach (var item in Model)
        {
            <partial name="_PetitionCard" model="item" />
        }
    </div>
    <div class="row justify-content-center">
        <a class="btn btn-danger mb-3 w-auto" asp-action="Create">Create New</a>
    </div>
</div>
